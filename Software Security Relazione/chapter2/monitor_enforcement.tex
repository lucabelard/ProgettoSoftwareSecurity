
% Updated RE Monitor Graphs with Primitives: halt, off, store, dump and Automata Style

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[->, >=stealth', shorten >=1pt, auto, node distance=4.5cm, semithick]
        \tikzstyle{every state}=[fill=blue!10,draw=black,text=black, minimum size=1.2cm]
        \tikzstyle{accepting}=[fill=red!20, text=red!80!black]

        % Safety Monitor S4 (Truncation Automaton style)
        \node[initial, state] (q0) {$q_0$};
        \node[state, accepting] (qErr) [right of=q0] {$q_{Err}$};

        \path 
            (q0) edge [loop above] node [align=center] {$\Sigma \setminus \{req\_pay\}$\\ \texttt{dump}} (q0)
            (q0) edge [loop below] node [align=center] {$req\_pay \land (P \ge 95\%)$\\ \texttt{dump}} (q0)
            (q0) edge node [align=center] {$req\_pay \land (P < 95\%)$\\ \texttt{halt}} (qErr)
            (qErr) edge [loop above] node [align=center] {$\Sigma$\\ \texttt{halt}} (qErr);
            
    \end{tikzpicture}
    \caption{Monitor di Runtime per la Proprietà di Safety S4. Definizione formale: $P = \{ (\Sigma \setminus \{req\_pay\})^* \cdot (req\_pay \land P \ge 0.95) \}^\omega$}
    \label{fig:safety_monitor}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[node distance=2.5cm, auto,
        block/.style={rectangle, draw, fill=green!10, text width=9em, text centered, rounded corners, minimum height=3em},
        decision/.style={diamond, draw, fill=yellow!10, text width=4.5em, text centered, inner sep=0pt},
        line/.style={draw, -latex', thick},
        cloud/.style={draw, ellipse, fill=red!10, node distance=2.5cm, minimum height=2em}]


        % Guarantee Monitor G1 (Edit Automaton style - Image based)
        \node[initial, state] (g1) {1};
        \node[state] (g2) [right of=g1] {2};
        \node[state, accepting] (g3) [right of=g2] {3}; % State R={3}

        \path 
            (g1) edge [loop above] node [align=center] {$\Sigma \setminus \{e\}$\\ \texttt{dump}} (g1)
            (g1) edge node [align=center] {$e$\\ \texttt{store}} (g2)
            (g2) edge [loop above] node [align=center] {$\Sigma \setminus \{p\}$\\ \texttt{store}} (g2)
            (g2) edge node [align=center] {$p$\\ \texttt{dump}} (g3)
            (g3) edge [loop above] node [align=center] {$\Sigma$\\ \texttt{dump}} (g3);
            
    \end{tikzpicture}
    \caption{Monitor di Runtime per la Proprietà di Guarantee G1. Definizione formale: $P = \{ (\Sigma \setminus \{e\})^* \cdot e \cdot (\Sigma \setminus \{p\})^* \cdot p \cdot \Sigma^\omega \}$ dove $e=$ evidenze, $p=$ pagamento.}
    \label{fig:guarantee_monitor}
\end{figure}
