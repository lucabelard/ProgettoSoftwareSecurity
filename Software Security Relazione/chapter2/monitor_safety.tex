\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[->, >=stealth', shorten >=1pt, auto, node distance=4.5cm, semithick]
        \tikzstyle{every state}=[fill=blue!10,draw=black,text=black, minimum size=1.2cm]
        \tikzstyle{accepting}=[fill=red!20, text=red!80!black]

        % Safety Monitor S4 (Truncation Automaton style with Check State)
        \node[initial, state] (q0) {$q_0$};
        \node[state] (q1) [right of=q0] {$q_1$};
        \node[state, accepting] (qErr) [right of=q1] {$q_{Err}$};

        \path 
            (q0) edge [loop above] node [align=center] {$\Sigma \setminus \{req\_pay\}$\\ \texttt{dump}} (q0)
            (q0) edge node [align=center] {$req\_pay$\\ \texttt{store}} (q1)
            (q1) edge [bend left] node [align=center] {$P \ge 95\%$\\ \texttt{dump}} (q0)
            (q1) edge node [align=center] {$P < 95\%$\\ \texttt{halt}} (qErr)
            (qErr) edge [loop above] node [align=center] {$\Sigma$\\ \texttt{halt}} (qErr);
            
    \end{tikzpicture}
    \caption{Monitor di Runtime per la Proprietà di Safety S4 con stato di verifica ($q_1$). Definizione formale: $P = \{ (\Sigma \setminus \{req\_pay\})^* \cdot (req\_pay \land P \ge 0.95) \}^\omega$}
    \label{fig:safety_monitor}
\end{figure}
